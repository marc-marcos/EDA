#include <iostream>
#include <set>

using namespace std;

int main()
{
    int max, min, n, sum = 0, total = 0;
    string action;

    while (cin >> action)
    {
        if (action == "delete") {

        }

        if ()
        if (total = 0)
        {
            sum = max = min = n;
            ++total;
        }

        else {
            if (n > max) {
                max = n;
                sum += n;
                ++total;
            }

            else if (n < min) {
                min = n;
                sum += n;
                ++total;
            }
        }
    }
}